<div class="bg-white border rounded-lg p-4 shadow-sm hover:shadow-md transition max-h-[150px] overflow-hidden">
  <div class="flex items-start justify-between">
    <div class="flex-1 pr-3">
      <h3 class="text-xl font-semibold text-teal-600 hover:underline">
        <%= link_to task.title, project_task_path(project, task), class: "block" %>
      </h3>
      <div class="text-sm text-gray-600 mt-2">
        <% if task.assigned_user %>
          Assigned to: <%= task.assigned_user.username %>
        <% else %>
          Unassigned
        <% end %>
      </div>
      <div class="text-sm text-gray-500 mt-2">
        <%= truncate(task.description.to_s, length: 80) %>
      </div>
    </div>

    <div class="text-right w-24">
      <div class="text-xs text-gray-500">Progress</div>
      <div class="mt-0 ml-3">
        <div class="w-full bg-gray-200 h-2 rounded overflow-hidden">
          <div style="width: <%= task.progress.to_i %>%;" class="h-2 bg-teal-500"></div>
        </div>
        <% if task.progress.present? %>
          <div class="text-xs text-gray-600 mt-1 ml-2"><%= task.progress %></div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="mt-2 flex items-center justify-between text-xs text-gray-400">
    <div>Due: <%= task.estimated_deadline.present? ? task.estimated_deadline.strftime("%b %d, %Y") : "—" %></div>
    <div><%= time_ago_in_words(task.created_at) %> ago</div>
  </div>
</div>
